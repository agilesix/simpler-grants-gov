// Every ported form, so one compile produces the whole artifact tree and the bank
// sees the complete set of questions they reach. Each form file compiles on its own.
import "./forms/sf424.tsp";
import "./forms/sf424-short.tsp";
import "./forms/sf424a.tsp";
